{
    "name": "pwanote",
    "version": "1.0.0",
    "description": "sami PWA",
    "main": "index.js",
    "scripts": {
        "start": "http-server -c-1",
        "start:ssl": "http-server -c-1 -S -C cert.pem -K key.pem",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "PWA",
        "sami"
    ],
    "author": "sami",
    "license": "MIT",
    "dependencies": {},
    "devDependencies": {
        "http-server": "^0.10.0"
    }
}
